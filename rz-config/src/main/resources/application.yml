server:
  port: 8889

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kimckm/config-repo.git
# 默认值是 http://localhost:8761/eureka/
# eureka:
#   client:
#     serviceUrl:
#       defaultZone: http://127.0.0.1:8761/eureka/
