server:
  port: 8889

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: /home/kim/conf

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8888/eureka/
